<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xingyu.demo.mapper.UserMapper">
	<resultMap type="com.xingyu.demo.pojo.User" id="userRM">
		<id property="id" column="id"/>
		<result property="name" column="name"/>
		<result property="age" column="age"/>
	</resultMap>

	<insert id="insert" parameterType="com.xingyu.demo.pojo.User">
		insert into user values (#{id}, #{name}, #{age});
	</insert>

	<select id="getById" parameterType="com.xingyu.demo.pojo.User" resultMap="userRM">
		select * from user where id = #{id}
	</select>

	<select id="getAll" resultMap="userRM">
		select * from user where 1=1
	</select>

</mapper>